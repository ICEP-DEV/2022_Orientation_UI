// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyC_H3yiLVvSFDJNZHe5VgTrVwDOMTTGbQw",
    databaseURL: 'https://orientation-pwa-default-rtdb.firebaseio.com',
    authDomain: "orientation-pwa.firebaseapp.com",
    projectId: "orientation-pwa",
    storageBucket: "orientation-pwa.appspot.com",
    messagingSenderId: "539690019804",
    appId: "1:539690019804:web:f755deb225ede1d22c7d19",
  },
};


/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
